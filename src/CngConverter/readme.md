# CNG Converter

Converts CNG files to JPG.

`dotnet run -c Release -- --cng-path <path to CNG files> --output-path <output path>`

**Output path will be deleted and recreated.**

Input path: It expects a directory containing the decade folders.
